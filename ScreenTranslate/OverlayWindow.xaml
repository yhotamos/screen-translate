<Window x:Class="ScreenTranslate.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Maximized">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30" 
                      ResizeBorderThickness="8" 
                      GlassFrameThickness="0" 
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 上部：WindowChromeのCaptionHeight領域。何もしなくてもドラッグ＆ダブルクリック最大化が効きます -->
        <Border Grid.Row="0" Background="#66FF0000" BorderBrush="Red" BorderThickness="2,2,2,0">
            <TextBlock Text="[DRAG TO MOVE / DOUBLE CLICK TO MAXIMIZE]" 
                       Foreground="White" FontSize="11" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       IsHitTestVisible="False"/>
        </Border>

		<!-- 下部：背景TransparentなのでOSレベルで背後に完全クリック透過します。赤い枠線のみ表示 -->
		<!-- ただし、端(ResizeBorderThickness=8)の領域だけは自動的にリサイズ反応します -->
		<Border Grid.Row="1" Background="Transparent" IsHitTestVisible="False">
			<Border BorderBrush="Red" BorderThickness="2,0,2,2" />
		</Border>

		<Rectangle x:Name="BorderRect" Grid.RowSpan="2" Stroke="Transparent" IsHitTestVisible="False"/>

		<!-- OCR矩形描画用のCanvas (画面全体に合わせるため Row=0, RowSpan=2 に変更) -->
		<Canvas x:Name="OverlayCanvas" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" Background="Transparent"/>

	</Grid>
</Window>